Flask==3.0.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
mysql-connector-python==8.4.0
python-dotenv==1.0.1
Werkzeug==3.0.3
waitress==3.0.0
requests==2.32.3
flask-socketio==5.6.1
reportlab==5.0.1
openpyxl==3.1.5
flask-talisman==1.1.0

# Production WSGI stack (see wsgi.py's module docstring for the gunicorn
# invocation these are for). Install fine on any platform, including
# Windows dev machines, but gunicorn itself only *runs* on POSIX — local
# dev doesn't need this stack at all, since `py app.py` serves the app
# via Flask-SocketIO's own threading mode (socketio.run()) instead.
gunicorn==26.2.0
gevent==26.8.0
gevent-websocket==0.10.1

simple-websocket==1.0.0

# Only needed if RATELIMIT_STORAGE_URI or SOCKETIO_MESSAGE_QUEUE is set to
# a redis:// URL (see extensions.py) — required for any multi-worker
# production deployment, not needed for single-worker/local dev.
# redis==5.0.8