// Partner-facing facts and quotes for the landing page, kept in one
// place like about.js. Both lists start EMPTY on purpose: anything left
// empty is simply not shown, so the page never displays a made-up claim.
//
// FACTS join the stats band under the packages (next to the real
// catalog/package counts the server provides). Keep `value` short (a
// word, a number, "3-5 days") and `label` a few words, e.g.
//   { value: 'Nationwide', label: 'delivery across the Philippines' },
//   { value: '3-5 days', label: 'from order to your door' },
//   { value: 'GCash', label: 'bank transfer or cash on pickup' },
//
// TESTIMONIALS show under the band once there's at least one real quote
// from a real partner (with their permission). Keep each quote to about
// two sentences, e.g.
//   { quote: '...', name: 'Full name', role: 'Reseller', place: 'Cebu City' },

export const FACTS = [];

export const TESTIMONIALS = [];
